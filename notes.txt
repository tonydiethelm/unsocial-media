Need to map a directory and return contents as an object?

Do I need to watch for changes to the directory? Or just update every X seconds?
fsPromises.watch(filename[, options])

Need to read the directory map and create HTML

Need to make a configuration page
fs.copyFile(src, dest[, mode], callback)
Don't copy, link instead. 

Need to serve app and config page

Need to make CSS for everything.

